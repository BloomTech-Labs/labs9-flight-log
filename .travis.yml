language: node_js
node_js:
  - "node"
cache:
  directories:
    - ./frontend/node_modules
script:
  - cd frontend
  - yarn install
  - yarn build
install:
  - npm install -g firebase-tools
after_success:
  - firebase deploy --token $FIREBASE_TOKEN